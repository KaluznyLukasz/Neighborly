# Architecture decisions

One file per decision: `YYYY-MM-DD-<slug>.md`. Create with `/nei-new-adr`.
A decision that changes how the app is built — a format choice, a dependency, a
pattern adopted or rejected. Superseded ADRs stay in place with `status: superseded`.
